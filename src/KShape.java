/**
 * An enum that makes it easier to handle the separate types of shapes
 * that will be randomly chosen. Plus it looks way nicer to refer to a
 * shape by name as opposed to some arbitrary number that's linked to
 * a particular shape type.
 */
public enum KShape {
    TRIANGLE,
    CIRCLE,
    QUAD,
    EPICYCLOID
}
